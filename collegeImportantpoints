arr[i][i] access diagnol element arr[i][-(i+1)] to access the diagnal elements of right diagnal.

#####

check bit

#########


fibnochi for large
factorial for large
#############

floyd patter-1
floyd patter -n
Given a string, check if it contains all the letters of the alphabet.
compress string with one variable less
patterns
###########
def longest_prefix_suffix(s):
    n = len(s)
    lis = [0] * n
    length = 0 
    i = 1
    while i < n:
        if s[i] == s[length]:
            length += 1
            lis[i] = length
            i += 1
        else:
            if length != 0:
                length = lis[length - 1]
            else:
                lis[i] = 0
                i += 1

    return lis[n - 1]

# Example usage
s = input()
result = longest_prefix_suffix(s)
print(result)
longest prefix suffix

###############
Pyramid Pattern



n = int(input())

# Print the upper half of the pattern
for i in range(1, n+1):
    # Print the first half of each row
    for j in range(1, i+1):
        print(chr(ord('A') + j - 1), end=" ")

    # Print the second half of each row in reverse order
    for j in range(i-1, 0, -1):
        print(chr(ord('A') + j - 1), end=" ")

    print()



things to do tommmorow are 

learn django 
learn javascript 
do dsa 
finish these topics imdeatily 
